import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  crossButton: {
    position: 'absolute',
    left: 15,
    top: 44,
  },
  markerMapButton: {
    position: 'absolute',
    right: 30,
    top: 44,
  },
});
