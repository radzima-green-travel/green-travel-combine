import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  leftButton: {
    position: 'absolute',
    left: 15,
    top: 44,
  },
  rightButton: {
    position: 'absolute',
    right: 30,
    top: 44,
  },
});
