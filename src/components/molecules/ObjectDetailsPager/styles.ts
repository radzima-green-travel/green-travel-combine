import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  pagerContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 16,
  },
});

export {styles};
