import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 73,
    right: 16,
  },
  showLocationButton: {
    marginTop: 8,
  },
  contentContainer: {
    paddingBottom: 16,
  },
});
