import React from 'react';

import {ErrorView} from 'molecules';
import {screenOptions} from './screenOptions';

export const ErrorScreen = () => {
  return <ErrorView />;
};

ErrorScreen.screenOptions = screenOptions;
