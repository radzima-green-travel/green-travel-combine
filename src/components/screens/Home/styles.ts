import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollSontainer: {
    backgroundColor: 'white',
  },
  contentContainer: {
    marginHorizontal: 16,
    marginTop: 42,
  },
});
