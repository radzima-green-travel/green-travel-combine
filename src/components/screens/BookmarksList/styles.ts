import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    paddingTop: 32,
  },
  cardContainer: {
    marginBottom: 32,
    marginHorizontal: 16,
  },
});
